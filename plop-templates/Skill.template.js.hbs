import React from "react";
import { SkillBar } from "../ui/SkillBar";

export const Skill{{pascalCase name}} = () => {
return (
<SkillBar percentage="{{percentage}}" mainColor="#{{mainColor}}" textColor="{{textColor}}">
  {{name}}
</SkillBar>
);
};
